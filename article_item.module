<?php

/**
 * Implementing hook_theme().
 */
function article_item_theme($existing, $type, $theme, $path) {

  return array(
    'article_list' => array(
      'variables' => array('items' => array(), 'title' => '')
    )
  );
}
